# Nova Current Truth

Date: 2026-04-18

## Purpose

This file is the current repo-facing truth index for active alignment work. It exists because several older status files are still useful as historical snapshots, but they are no longer reliable as the current runtime baseline.

## Trust Order

When sources disagree, use this order:

1. Live runtime behavior and active shell delegation.
2. `This_is_nova` recovery checkpoints.
3. Current checked-in service wiring and active tests.
4. Current control assets.
5. Current alignment audits in `docs/`.
6. Older status/baseline markdowns.
7. Archive docs, generated artifacts, and legacy exact-behavior tests.

## Current Cross-Checked Reality

- Nova is not missing architecture. The main remaining problem is alignment drift at the gravity wells.
- The two highest-risk drift surfaces remain `nova_http.py` and `nova_core.py`, but `nova_http.py` is now materially cleaner after the first alignment pass and `nova_core.py` now has a real service-backed alignment pass underway across identity, classifiers, reply-contract wiring, truthful-limit shaping, truth-hierarchy routing, identity-history outcomes, retrieval followups, session followups, open-fallback/open-probe behavior, the broader developer-profile/profile-followup family, the developer-profile support helper family, action-history presentation, the action-ledger lifecycle wrappers, the reflection-health helper/orchestrator family, the routing-support decision/bypass/followup helper family, the conversation-followup helper family including conversation/retrieval state construction, the fuller identity-preference helper family, the general turn-helper family, the active keyword/tool shortcut seam, the location/weather helper family, the registered supervisor-rule execution wrapper and the active CLI loop's supervisor-intent application block, the broader memory-learning family including stats/add/recall, recent-learned summaries, stats/audit rendering, earlier-memory prefixing, and memory-event recording, the legacy CLI deterministic turn-outcome cluster, which now routes through the shared `services/nova_turn_outcomes.py` owner instead of hand-applying the same branches inside `nova_core.py`, the remaining CLI profile/preferences reply cluster plus the active CLI loop's profile/preference sequence replies and color-animal match branch, which now route through `services/nova_profile_followups.py`, the active CLI loop's sequence reply normalization and sequence-result application block, which now route through `services/nova_cli_sequence.py`, and the long deterministic-content block plus fallback-preparation / low-confidence gating and final LLM fallback execution inside `services/nova_reply_sequence.py`, which now route through `services/nova_reply_deterministic.py` and `services/nova_fallback_flow.py` instead of keeping planner flow and deterministic reply ownership mixed in the same function. `services/nova_reply_sequence.py` now also treats planner-before-deterministic mode as one planner attempt instead of double-invoking planner around the same turn. The active CLI loop now reaches that earlier planner path through the explicit `services/nova_cli_sequence.py` handoff instead of setting those flags inline in the loop body. The CLI fallback orchestration block now routes through `services/nova_fallback_flow.py` for pending-weather fallback, policy-gate blocking, low-confidence blocking, context preparation, and final LLM fallback reply shaping, the planned-tool dispatch wrapper, which now routes through `services/nova_tool_dispatch.py` instead of keeping that tool-selection logic inline in `nova_core.py`, the command-handler entrypoint, which now delegates immediately to `services/nova_command_handlers.py` even though the old inline body still remains below it as unreachable cleanup, and the CLI loop entrypoint, which now delegates immediately to `services/nova_cli_loop.py` even though the old inline body still remains below it as unreachable cleanup.
- The control/runtime plane is comparatively cleaner and already strongly service-shaped.
- Work Tree / Scheduled Tree remains a real product surface and should not be treated as optional or legacy.
- `autonomy_maintenance.py` now has a first direct bridge from subconscious output into Work Tree, but the policy is no longer buried there: [services/subconscious_work_tree_triage.py](</C:/Nova/services/subconscious_work_tree_triage.py>) now owns the owner-classification, review contract, review context, and review gate for subconscious priorities, and [services/subconscious_review_authority.py](</C:/Nova/services/subconscious_review_authority.py>) now consumes that `review_contract` plus signal-specific `review_context` to produce the promotion verdict before Work Tree ingestion. Maintenance translates `training_priorities` into normalized `subconscious_candidate` signals, routes them through the triage service, routes approved candidates through the review-authority service, ingests only authority-approved candidates through `WORK_TREE_SIGNAL_INGESTION_SERVICE`, and summarizes the result in `last_subconscious_signal_ingestion`. High-confidence owner-aware pressure becomes Work Tree review with an explicit `review_contract`; ungrounded or deferred pressure stays in maintenance state as `triage_skipped`, and contract-level rejections stay recorded as `authority_skipped`. When a low-priority or medium-priority candidate is already staged on a live Work Tree branch, maintenance can now refresh that existing branch instead of only skipping it, which lets repeated subconscious pressure strengthen branch notes/priority/evidence instead of churning duplicate review work. The review-authority layer is now mixed live-organ / route-backed for the mapped subconscious pressure signals: it can consult Nova's live fulfillment viability path directly, it can attempt live supervisor-rule ownership directly, it can consult live supervisor reflection probes for richer red/yellow review signals, and it falls back to Nova's real `_probe_turn_routes` path when the live supervisor organ does not claim the turn. Review context is derived from actual subconscious family/variation metadata when that metadata is available, and the triage layer can now recover that family context by `suggested_test_name` as well as by signal. Family classification is also less flat now: explicit supervisor-owned fallthrough seams like weather continuation, memory capture, retrieval followup, patch routing, and session fact recall are classified by seam ownership first instead of being reduced to a generic route-comparison bucket just because the pressure signal is `fallback_overuse`. The bridge also now carries a compact live runtime snapshot from maintenance state into the review context, and the authority layer can defer low-priority promotion when live runtime pressure is already high (for example failed regression, failed generated queue work, and waiting Work Tree backlog). It is also now backlog-aware enough to suppress low-priority re-promotion when a matching subconscious candidate is already staged in Work Tree from a prior maintenance cycle, using real branch/task detail rather than only the last maintenance summary. This authority path is now verified on more than one real subconscious family, including fulfillment-fallthrough, weather-continuation, memory-capture, retrieval-followup, patch-routing, and session-fact-recall pressure. The Work Tree cycle itself also now runs hotter than the earlier inspection-only posture: maintenance raises the cycle ceiling from `3` to `10` steps and allows autorun on a broader set of real branch tools including `web_fetch`, `web_search`, `web_research`, `weather_current_location`, `weather_location`, `location_coords`, `update_now`, and `patch_rollback` while still leaving `patch_apply` outside the autorun lane. Work Tree itself now recognizes `test_...` review tasks as search targets, recognizes metadata/symbol-reference inspection as `find` work instead of stale `read` file paths, and can rebalance old branch tool choices before execution, which removed the fake `Not a file:` failures on subconscious review branches and let the signal-ingestion tree clear the previously stuck route-comparison branches. The control UI also now refreshes Work Tree state from persisted storage before serving `/api/control/work-trees`, so the page shows completed review branches instead of stale in-memory branch snapshots from the web process. The generated queue path is also live again: a real generated session that had been failing now completes green after repairing the reflection/action-ledger shell drift in `nova_core.py`, and kidney dry-run calls no longer overwrite the last real enforce status payload. This is a real start, but it is not the full loop yet because fulfillment/supervisor still are not direct runtime reviewers behind every authority decision.
- Repo-facing status surfaces from March 2026 and some audit snapshots from April 2026 are historical context, not current authority.
- The live autonomy loop is now producing visible throughput instead of only staging work. Generated queue execution now runs in small batches of up to `3` sessions per maintenance cycle and records batch counters in `last_generated_queue_run`. Recent live cycles pushed the generated queue down into single digits (`queue_open_count: 9` and then `8`) while still executing real Work Tree steps, and the current generated queue has now been cleared back to `0` open / `0` actionable. Kidney can now mark stale generated definitions as `retired_locked` when Windows refuses to release the file handle; the generated queue honors that retirement record by fingerprint, so stale locked definitions stop polluting the live queue even if the source file cannot be removed yet. Patch preview tempdir failures no longer crash the whole maintenance cycle; they degrade into recorded `preview_permission_error` outcomes instead. Subconscious review branches are also starting to carry family-aware guidance instead of generic review text: triage now emits family-specific review focus / next-task language for patch-routing, retrieval-followup, memory-capture, weather-continuation, session-fact-recall, and fulfillment/route-comparison families, Work Tree preserves that guidance in branch notes, and the control UI can now show those notes in branch inspection.
- `nova_core.py` line-count checkpoint after the drift repair on April 20, 2026: working tree `11,094` after the first wrapper restore, `10,748` after the second service-wrapper restore, `10,611` after restoring the identity-history / action-ledger / developer-location wrapper cluster, `10,505` after restoring the adjacent profile-followup wrapper cluster, `10,470` after restoring the adjacent profile-state wrapper cluster, `10,363` after restoring the adjacent developer-profile support wrapper cluster, `10,336` after restoring the adjacent shared developer-profile helper wrappers, `9,047` after the second drift-repair pass restored the CLI delegations and trimmed the unreachable command/loop bodies, `8,967` after restoring the weather/location and adjacent turn-helper wrapper cluster, `8,889` after restoring the self identity-preferences helper cluster, `8,836` after restoring the developer-preferences helper cluster, `8,800` after collapsing the live `ollama_chat` wrapper onto `services/nova_ollama_chat.py`, `8,698` after collapsing `_handle_supervisor_intent()` onto `services/nova_supervisor_flow.py`, `8,514` after restoring the web fetch / web search helper batch, `8,254` after collapsing `tool_web_search()` and `tool_web_research()` onto `services/nova_web_tools.py`, `8,164` after collapsing `tool_weather()` and `kb_search()` onto `services/nova_location_weather.py` and `services/nova_knowledge_packs.py`, `8,122` after collapsing `_build_local_topic_digest_answer()` onto `services/nova_knowledge_packs.py`, `8,071` after collapsing `probe_search_endpoint()` onto `services/nova_search_endpoint.py`, `8,004` after collapsing `_consume_conversation_followup()` onto `services/nova_followup_dispatch.py`, `7,973` after fresh-extracting the legacy `web_search()` / `tool_search()` pair into `services/nova_web_tools.py`, and `7,771` after extracting the pulse/status cluster into `services/nova_pulse.py`; `HEAD` baseline remains `11,398`. Treat future core cleanup passes as suspect unless they keep wrapper integrity intact and line count stays explainable.

## Current Alignment Documents

- `docs/ALIGNMENT_AUDIT_2026-04-18.md`
- `docs/NOVA_HTTP_ALIGNMENT_AUDIT_2026-04-18.md`
- `docs/NOVA_CORE_ALIGNMENT_AUDIT_2026-04-18.md`

## Historical, Not Canonical

These remain useful, but should not be treated as the current source of truth without cross-checking:

- `PROJECT_STATUS.md`
- `docs/archive/audits/2026-04-17/`
- `docs/STATUS.md`
- `docs/archive/`
- `tests/legacy/`

## Immediate Alignment Priorities

1. Treat the first `nova_http.py` alignment pass as established progress and avoid drifting back to old hidden wiring patterns.
2. Treat the first `nova_core.py` shell-alignment pass as materially established: the seam-lock set is now clean across hard-answer, memory, patching, ollama-chat, supervisor-intent, search-endpoint, knowledge-pack, web-search, and conversation-followup wrappers, so the next core work should be broader shell reduction or behavior-level stabilization rather than re-fighting those ownership seams.
3. Treat the self-improvement loop as partially connected rather than missing: subconscious, Work Tree, kidney, and maintenance are all real, and maintenance now promotes only triage-approved subconscious priorities into Work Tree candidates while retaining weaker/ungrounded pressure in maintenance state. Fulfillment/supervisor still needs to become the clearer decision membrane above that maintenance gate.
3. Keep stale status snapshots clearly labeled as historical so they stop competing with current recovery truth.
4. Keep generated runtime/session artifacts from being mistaken for curated source truth.
